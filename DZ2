#include <iostream>
#include <cmath>
using namespace std;

main()
{	float a, b, x, y, z, q, sinusx;
	cout << "Введите значения X и Y" << endl;
	cin >> x >> y;
	cout << "Введенные: X = " << x << " è Y = " << y << endl;
	a = (2 + y) / (pow(x,2)) + x;
	b = 1/ (sqrt(pow(x,2)) + 10);
	z = a / b;
	cout << "Ответ:" << z << endl;
	cin.get();
	cin.get();
